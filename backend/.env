PORT=5000

# MongoDB Connection String
# For local MongoDB (if installed):
# MONGODB_URI=mongodb://localhost:27017/resume-builder

# For MongoDB Atlas (cloud):
# Replace <username>, <password>, and <cluster-url> with your actual values
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/resume-builder?retryWrites=true&w=majority

# Temporary - we'll add this later when we set up AI features
# ANTHROPIC_API_KEY=your_claude_api_key_here

# For now, use local MongoDB (comment this out if you're using Atlas)
MONGODB_URI=mongodb://localhost:27017/resume-builder